# 🚀 Инструкция для первого запуска

## 📋 Чек-лист перед запуском

### ✅ Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

### ✅ Шаг 2: Создание .env файла

#### Windows:
```cmd
copy env.example .env
```

#### Linux/Mac:
```bash
cp env.example .env
```

### ✅ Шаг 3: Получение токенов

#### 3.1. BOT_TOKEN (для админ-панели)
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен → добавьте в `.env`

```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

#### 3.2. API_ID и API_HASH (для парсера)
1. Откройте [https://my.telegram.org](https://my.telegram.org)
2. Войдите с номером телефона
3. "API development tools"
4. Создайте приложение
5. Скопируйте `api_id` и `api_hash` → добавьте в `.env`

```env
API_ID=12345678
API_HASH=0123456789abcdef0123456789abcdef
```

#### 3.3. Ваш Telegram ID
1. Откройте [@username_to_id_bot](https://t.me/username_to_id_bot)
2. Отправьте `/start`
3. Скопируйте ваш ID → добавьте в `.env`

```env
ADMIN_IDS=123456789
```

### ✅ Шаг 4: Авторизация технического аккаунта

**⚠️ ВАЖНО:** Используйте отдельный технический аккаунт, НЕ основной!

#### Способ 1: Через первый запуск
```bash
python worker.py
```

Введите:
- Номер телефона (с кодом страны, например: +79001234567)
- Код из Telegram
- Пароль 2FA (если включен)

После успешной авторизации нажмите `Ctrl+C`.

#### Способ 2: Через генератор SESSION_STRING
```bash
python generate_session.py
```

Скопируйте полученную строку и добавьте в `.env`:
```env
SESSION_STRING=полученная_строка
```

### ✅ Шаг 5: Первый запуск

#### Windows:
```cmd
start.bat
```
или
```cmd
python run.py
```

#### Linux/Mac:
```bash
chmod +x start.sh
./start.sh
```
или
```bash
python run.py
```

### ✅ Шаг 6: Настройка через бота

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Выберите "📊 Парсер / Лидогенератор"

#### 6.1. Добавьте ключевые слова
Выберите "🔑 Ключ-слова" → отправьте слова в чат

**Примеры:**
- `продам` — найдет любые сообщения со словом "продам"
- `_айфон_` — найдет только отдельное слово "айфон"
- `продам+айфон` — найдет сообщения с ОБОИМИ словами

#### 6.2. Добавьте стоп-слова (опционально)
Выберите "⛔ Стоп-слова" → отправьте слова для исключения

**Примеры:**
- `барахолка`
- `куплю`
- `битый`

#### 6.3. Укажите чат для уведомлений
Выберите "📢 Чат для уведомлений" → отправьте ID

**Как узнать ID:**
- Ваш ID: [@username_to_id_bot](https://t.me/username_to_id_bot)
- ID группы: добавьте бота в группу, отправьте `/id`

#### 6.4. Включите парсер
Нажмите "🔴 Работает" → статус изменится на "🟢 Работает"

---

## 🎉 Готово!

Парсер запущен и будет находить сообщения по вашим ключевым словам!

---

## 🧪 Тестирование

### Проверка работы парсера

1. Убедитесь, что технический аккаунт подписан на группы/каналы
2. Напишите тестовое сообщение в группу с вашим ключевым словом
3. Через несколько секунд должно прийти уведомление

### Если ничего не работает

#### 1. Проверьте логи
Посмотрите в терминале, где запущен парсер. Там будут ошибки, если что-то не так.

#### 2. Проверьте настройки
```
✅ Парсер включен? (🟢 Работает)
✅ Ключевые слова добавлены?
✅ ID чата указан?
✅ Технический аккаунт авторизован?
✅ Группы/каналы включены?
```

#### 3. Проверьте подписки
Технический аккаунт должен быть подписан на группы, которые вы хотите парсить.

#### 4. Проверьте .env
```env
BOT_TOKEN=... (должен быть заполнен)
API_ID=... (должен быть заполнен)
API_HASH=... (должен быть заполнен)
ADMIN_IDS=... (ваш ID)
```

---

## 📚 Дополнительная информация

- [README.md](README.md) - полная документация
- [QUICKSTART.md](QUICKSTART.md) - быстрый старт
- [FAQ.md](FAQ.md) - частые вопросы
- [EXAMPLES.md](EXAMPLES.md) - примеры использования

---

## 🆘 Получить помощь

Если возникли проблемы:
1. Прочитайте [FAQ.md](FAQ.md)
2. Проверьте логи на ошибки
3. Создайте Issue в репозитории

---

**Успешного парсинга! 🚀**

