# ❓ Часто задаваемые вопросы (FAQ)

## 🔧 Установка и настройка

### Как получить BOT_TOKEN?

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Придумайте имя и username для бота
4. Скопируйте полученный токен

### Как получить API_ID и API_HASH?

1. Перейдите на [https://my.telegram.org](https://my.telegram.org)
2. Войдите с помощью номера телефона
3. Нажмите "API development tools"
4. Заполните форму (можно написать что угодно)
5. Скопируйте `api_id` и `api_hash`

### Что такое SESSION_STRING и зачем он нужен?

SESSION_STRING — это зашифрованная строка с данными авторизации вашего технического аккаунта. Он позволяет не вводить код авторизации каждый раз при запуске парсера.

**Способы получить:**
- Оставить пустым в `.env` — при первом запуске введете код
- Сгенерировать через `python generate_session.py`

### Могу ли я использовать свой основной аккаунт?

**НЕ РЕКОМЕНДУЕТСЯ!** Используйте отдельный технический аккаунт по нескольким причинам:
- Безопасность (в случае утечки session не пострадает основной аккаунт)
- Telegram может временно ограничить аккаунт за активный парсинг
- Чистота истории сообщений

### Как создать отдельный технический аккаунт?

1. Купите дешевую SIM-карту или используйте виртуальный номер
2. Зарегистрируйте новый аккаунт Telegram
3. Подпишите его на нужные группы и каналы
4. Используйте этот аккаунт для парсинга

## 🚀 Запуск и работа

### Парсер не запускается, что делать?

Проверьте:
1. ✅ Установлены ли зависимости: `pip install -r requirements.txt`
2. ✅ Создан ли файл `.env` с правильными токенами
3. ✅ Авторизован ли технический аккаунт
4. ✅ Нет ли ошибок в логах

### Как запустить парсер в фоне (на сервере)?

#### Linux (screen)
```bash
screen -S parser
python run.py
# Нажмите Ctrl+A, затем D чтобы отключиться
# Вернуться: screen -r parser
```

#### Linux (tmux)
```bash
tmux new -s parser
python run.py
# Нажмите Ctrl+B, затем D чтобы отключиться
# Вернуться: tmux attach -t parser
```

#### Linux (systemd service)
Создайте `/etc/systemd/system/telegram-parser.service`

### Как остановить парсер?

- **Запущен в терминале:** Нажмите `Ctrl+C`
- **Запущен через screen/tmux:** Подключитесь к сессии и нажмите `Ctrl+C`
- **Запущен как сервис:** `systemctl stop telegram-parser`

### Парсер запущен, но не находит сообщения. Почему?

Проверьте:
1. ✅ Включен ли парсер (🟢 Работает в админ-панели)
2. ✅ Добавлены ли ключевые слова
3. ✅ Установлен ли ID чата для уведомлений
4. ✅ Подписан ли технический аккаунт на группы/каналы
5. ✅ Включены ли группы/каналы в настройках
6. ✅ Не блокируют ли стоп-слова все сообщения

## 🔍 Фильтры и ключевые слова

### Как работают ключевые слова?

- **`продам`** — найдет "продам", "продаю", "продается" и т.д. (подстрока)
- **`_продам_`** — найдет только отдельное слово "продам"
- **`продам+айфон`** — найдет сообщения, содержащие ОБА слова

### Регистр имеет значение?

**Нет**, поиск НЕ чувствителен к регистру. "Айфон", "айфон", "АЙФОН" — это одно и то же.

### Можно ли использовать регулярные выражения?

В текущей версии — нет. Используйте комбинации:
- `_слово_` — точное слово
- `слово1+слово2` — оба слова
- Добавьте несколько вариантов написания

### Как исключить сообщения от ботов?

Парсер **автоматически игнорирует** сообщения от ботов.

### Как исключить конкретного пользователя?

1. Узнайте его Telegram ID через [@username_to_id_bot](https://t.me/username_to_id_bot)
2. Добавьте ID в **Черный список** в админ-панели

### Что делать, если приходят дубликаты?

Включите **"Игнор дублей"** в настройках парсера. Парсер будет игнорировать одинаковые сообщения за последние 24 часа.

## 📢 Уведомления

### Как узнать ID чата для уведомлений?

**Для личного чата:**
1. Откройте [@username_to_id_bot](https://t.me/username_to_id_bot)
2. Отправьте `/start`
3. Скопируйте ваш ID

**Для группы/канала:**
1. Добавьте [@username_to_id_bot](https://t.me/username_to_id_bot) в группу
2. Отправьте в группе `/id`
3. Скопируйте ID чата (обычно начинается с `-100`)

### Уведомления не приходят. Что делать?

Проверьте:
1. ✅ Правильно ли указан ID чата в настройках
2. ✅ Если это группа — добавлен ли бот в группу как администратор
3. ✅ Не заблокировали ли вы бота
4. ✅ Работает ли парсер (посмотрите логи)

### Можно ли отправлять в несколько чатов?

В текущей версии — нет. Но вы можете:
- Отправлять в группу, а затем пересылать вручную
- Доработать код в `worker.py`, добавив список чатов

### Как изменить формат уведомлений?

Отредактируйте функцию `send_lead_notification` в `worker.py`:

```python
notification_text = (
    "🔥 <b>Новое сообщение</b>\n\n"
    f"ID пользователя: <code>{sender_id}</code>\n"
    # ... измените здесь
)
```

## 🗄️ База данных

### Где хранятся данные?

В файле `parser.db` (SQLite) в корне проекта.

### Можно ли посмотреть базу данных?

Да, используйте любой SQLite клиент:
- [DB Browser for SQLite](https://sqlitebrowser.org/) (GUI)
- `sqlite3 parser.db` (командная строка)

### Как сделать бэкап?

Просто скопируйте файл `parser.db`:

```bash
# Linux/Mac
cp parser.db parser_backup_$(date +%Y%m%d).db

# Windows
copy parser.db parser_backup_%date:~-4,4%%date:~-10,2%%date:~-7,2%.db
```

### Как очистить базу данных?

```bash
# Удалить базу и она создастся заново
rm parser.db

# Или очистить таблицы через админ-панель
# "Удалить все" в каждом разделе
```

### Как перенести базу на другой сервер?

1. Остановите парсер
2. Скопируйте `parser.db` на новый сервер
3. Скопируйте `.env` (или настройте заново)
4. Запустите парсер

## 🔐 Безопасность

### Безопасно ли это?

Да, если соблюдать правила:
- ✅ Используйте отдельный технический аккаунт
- ✅ Не публикуйте `.env` файл
- ✅ Храните `parser_session.session` в безопасности
- ✅ Используйте сильные пароли

### Может ли мой аккаунт заблокировать Telegram?

Теоретически — да, если:
- ❌ Слишком активный парсинг (тысячи сообщений в минуту)
- ❌ Спам в личные сообщения
- ❌ Нарушение правил Telegram

**Как избежать:**
- ✅ Используйте отдельный технический аккаунт
- ✅ Не спамьте в личку пользователям
- ✅ Парсите только публичную информацию

### Безопасно ли хранить SESSION_STRING в .env?

**Условно безопасно**, если:
- ✅ `.env` не публикуется в репозитории (добавлен в `.gitignore`)
- ✅ Сервер защищен
- ✅ Нет доступа у посторонних

**Альтернатива:** Используйте файл сессии `parser_session.session` и не храните SESSION_STRING.

## 📊 Производительность

### Сколько групп можно парсить одновременно?

Telethon автоматически слушает все группы и каналы, на которые подписан аккаунт. Ограничений на количество нет.

### Замедляет ли парсер работу Telegram?

Нет, парсер работает полностью независимо от вашего основного клиента Telegram.

### Сколько ресурсов потребляет?

**Минимальные:**
- RAM: ~50-100 MB
- CPU: ~1-5%
- Диск: ~5-10 MB (база данных растет со временем)

### Можно ли запустить на Raspberry Pi?

**Да!** Проект легкий и отлично работает на:
- Raspberry Pi (любая модель)
- VPS с 512 MB RAM
- Дешевом хостинге

## 🛠️ Разработка

### Можно ли добавить свои функции?

**Конечно!** Проект открыт и легко расширяем:
- `bot.py` — добавьте новые команды в бота
- `worker.py` — измените логику фильтрации
- `database.py` — добавьте новые таблицы

### Как добавить новое ключевое слово программно?

```python
from database import Database

db = Database("parser.db")
db.add_keyword("ваше+ключевое+слово")
```

### Как получить доступ к логам?

Логи выводятся в консоль. Чтобы сохранить в файл:

```bash
python run.py > parser.log 2>&1
```

### Можно ли использовать PostgreSQL вместо SQLite?

Да, но потребуется переписать `database.py`. SQLite достаточно для большинства случаев.

## 📝 Прочее

### Где получить помощь?

1. Прочитайте [README.md](README.md)
2. Посмотрите [EXAMPLES.md](EXAMPLES.md)
3. Проверьте [FAQ.md](FAQ.md) (этот файл)
4. Создайте Issue в репозитории

### Можно ли использовать в коммерческих целях?

Да, проект под лицензией MIT. Но соблюдайте:
- Правила Telegram
- Законы вашей страны
- Политику конфиденциальности

### Будут ли обновления?

Проект активно развивается. Планируются:
- ✨ Импорт источников (автоподписка)
- ✨ Исходящие сообщения
- ✨ Веб-интерфейс
- ✨ Статистика и аналитика

### Как поддержать проект?

- ⭐ Поставьте звезду на GitHub
- 🐛 Сообщайте об ошибках
- 💡 Предлагайте идеи
- 🔧 Присылайте Pull Request'ы

---

**Не нашли ответ?** Создайте Issue в репозитории!

